What I've done so far…


installed composer

installed ffmpeg in my home directory.

installed ffmpeg in my project root (not sure if it's needed there.)

Added downloaded ffmpeg and ffprobe binaries into the project ffmpeg installation.
manually edited the php-ffmpeg ::create binaries locations.

Added a copy of those binaries to the home did ffmpeg install as well.

installed ffmpeg2theora in usr/local/bin

copied above to ffmpeg/ffmpeg/